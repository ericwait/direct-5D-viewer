% CaptureWindow - CaptureWindow() This will capture the current view to either the current directory or the directory specified in 'SetCapturePath'.
function CaptureWindow()
    D3d.Viewer.Mex('CaptureWindow');
end
