% ZoomDecrement - This will zoom the camera out.
%    Viewer.ZoomDecrement(speedFactor)
%    	SpeedFactor -- this is a multiplier on how fast or how big the steps are for each zoom.
function ZoomDecrement(speedFactor)
    D3d.Viewer.Mex('ZoomDecrement',speedFactor);
end
