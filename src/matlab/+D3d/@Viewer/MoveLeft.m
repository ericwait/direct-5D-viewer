% MoveLeft - This will move the camera left by one step.
%    Viewer.MoveLeft(speedFactor)
%    	SpeedFactor -- this is a multiplier on how fast or how big the steps are for each move.
function MoveLeft(speedFactor)
    D3d.Viewer.Mex('MoveLeft',speedFactor);
end
