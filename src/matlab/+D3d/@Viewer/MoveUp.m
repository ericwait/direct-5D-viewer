% MoveUp - This will move the camera up by one step.
%    Viewer.MoveUp(speedFactor)
%    	SpeedFactor -- this is a multiplier on how fast or how big the steps are for each move.
function MoveUp(speedFactor)
    D3d.Viewer.Mex('MoveUp',speedFactor);
end
