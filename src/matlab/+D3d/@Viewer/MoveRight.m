% MoveRight - This will move the camera right by one step.
%    Viewer.MoveRight(speedFactor)
%    	SpeedFactor -- this is a multiplier on how fast or how big the steps are for each move.
function MoveRight(speedFactor)
    D3d.Viewer.Mex('MoveRight',speedFactor);
end
