% MoveCamera - This will move the camera by the specified delta [x,y,z].
%    Viewer.MoveCamera(deltas)
%    	Deltas -- specifies the amount to move the camera in [x,y,z].
function MoveCamera(deltas)
    D3d.Viewer.Mex('MoveCamera',deltas);
end
