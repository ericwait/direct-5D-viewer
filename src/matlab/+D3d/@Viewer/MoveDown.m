% MoveDown - This will move the camera down by one step.
%    Viewer.MoveDown(speedFactor)
%    	SpeedFactor -- this is a multiplier on how fast or how big the steps are for each move.
function MoveDown(speedFactor)
    D3d.Viewer.Mex('MoveDown',speedFactor);
end
