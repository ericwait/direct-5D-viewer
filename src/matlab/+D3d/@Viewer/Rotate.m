% Rotate - This will start the volume rotating about the view aligned y axis.
%    Viewer.Rotate(on)
%    	On - If this is set to one, then the volume will rotate. If this is 0, the volume will not rotate.
function Rotate(on)
    D3d.Viewer.Mex('Rotate',on);
end
